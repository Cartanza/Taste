{{!--
    Parameters:
    * layout (wide, side)
    * background (color, image)
    * action (button, email, search)
--}}

{{#if @root.site.description}}
    {{> partials/components/header-content}}
{{else}}
    {{#if @root.site.cover_image}}
        {{> partials/components/header-content}}
    {{else}}    
        {{#if action}}
            {{> partials/components/header-content}}
        {{/if}}
    {{/if}}
{{/if}}
